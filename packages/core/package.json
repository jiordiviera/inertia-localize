{
  "name": "@inertia-localize/core",
  "version": "0.1.0",
  "description": "Framework-independent translation utilities for Inertia Localize",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:types": "tsc --noEmit -p tsconfig.types.json",
    "test": "pnpm build && pnpm check:types && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
