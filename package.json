{
  "name": "inertia-localize",
  "private": true,
  "packageManager": "pnpm@12.5.1",
  "scripts": {
    "build": "pnpm --recursive --if-present build",
    "test": "pnpm --recursive --if-present test",
    "lint": "pnpm --recursive --if-present lint"
  }
}
